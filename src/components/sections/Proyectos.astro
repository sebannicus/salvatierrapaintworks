---
interface Proyecto {
  src: string;
  alt: string;
  categoria: string;
  tipo: 'img' | 'video';
  label?: string;
}

const proyectos: Proyecto[] = [
  // Cabaña
  { src: '/images/cabaña-1.webp',  alt: 'Cabaña — Fachada',           categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-2.webp',  alt: 'Cabaña — Barnizado',         categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-3.webp',  alt: 'Cabaña — Detalle vigas',     categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-4.webp',  alt: 'Cabaña — Exterior',          categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-5.webp',  alt: 'Cabaña — Protección madera', categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-6.webp',  alt: 'Cabaña — Resultado Final',   categoria: 'cabana', tipo: 'img', label: 'Después' },
  { src: '/images/cabaña-7.webp',  alt: 'Cabaña — Vista Lateral',     categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-8.webp',  alt: 'Cabaña — Acabado Brillo',    categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-9.webp',  alt: 'Cabaña — Terminaciones',     categoria: 'cabana', tipo: 'img' },
  { src: '/images/cabaña-10.webp', alt: 'Cabaña — Vigas a la vista',  categoria: 'cabana', tipo: 'img' },

  // Casa
  { src: '/images/casa-antes1.webp',       alt: 'Casa — Fachada Inicial',  categoria: 'casa', tipo: 'img',   label: 'Antes' },
  { src: '/images/casa-despues1.webp',      alt: 'Casa — Fachada Renovada', categoria: 'casa', tipo: 'img',   label: 'Después' },
  { src: '/images/casa-antes2.webp',        alt: 'Casa — Fachada Inicial',  categoria: 'casa', tipo: 'img',   label: 'Antes' },
  { src: '/images/casa-despues2.webp',      alt: 'Casa — Fachada Terminada',categoria: 'casa', tipo: 'img',   label: 'Después' },
  { src: '/images/casa-despues3.webp',      alt: 'Casa — Acabado Detalle',  categoria: 'casa', tipo: 'img',   label: 'Después' },
  { src: '/images/video-interior-casa.mp4', alt: 'Casa — Video Interior',   categoria: 'casa', tipo: 'video', label: 'Video' },

  // Oficina
  { src: '/images/pintura-oficina-1.webp', alt: 'Oficina — Preparación',   categoria: 'oficina', tipo: 'img' },
  { src: '/images/pintura-oficina-2.webp', alt: 'Oficina — Empastado',     categoria: 'oficina', tipo: 'img' },
  { src: '/images/pintura-oficina-3.webp', alt: 'Oficina — Pintura',       categoria: 'oficina', tipo: 'img' },
  { src: '/images/pintura-oficina-4.webp', alt: 'Oficina — Proceso',       categoria: 'oficina', tipo: 'img' },
  { src: '/images/pintura-oficina-5.webp', alt: 'Oficina — Techos',        categoria: 'oficina', tipo: 'img' },
  { src: '/images/pintura-oficina-6.webp', alt: 'Oficina — Final',         categoria: 'oficina', tipo: 'img', label: 'Después' },
  { src: '/images/pintura-oficina-7.webp', alt: 'Oficina — Vista General', categoria: 'oficina', tipo: 'img' },
  { src: '/images/pintura-oficina-8.webp', alt: 'Oficina — Entrega',       categoria: 'oficina', tipo: 'img' },

  // Edificio
  { src: '/images/pintura-edificio-1.webp',  alt: 'Edificio — Fachada',            categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-3.webp',  alt: 'Edificio — Preparación',        categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-4.webp',  alt: 'Edificio — Aplicación Airless', categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-5.webp',  alt: 'Edificio — Resultado Final',    categoria: 'edificio', tipo: 'img', label: 'Después' },
  { src: '/images/pintura-edificio-6.webp',  alt: 'Edificio — Detalle Terminación',categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-7.webp',  alt: 'Edificio — Pintura Exterior',   categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-8.webp',  alt: 'Edificio — Vista Lateral',      categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-9.webp',  alt: 'Edificio — Acabado Impecable',  categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-10.webp', alt: 'Edificio — Fachada Comercial',  categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-11.webp', alt: 'Edificio — Detalles Finos',     categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-12.webp', alt: 'Edificio — Proyecto Terminado', categoria: 'edificio', tipo: 'img', label: 'Después' },
  { src: '/images/pintura-edificio-15.webp', alt: 'Edificio — Vista General',      categoria: 'edificio', tipo: 'img' },
  { src: '/images/pintura-edificio-14.mp4',  alt: 'Edificio — Video Proceso',      categoria: 'edificio', tipo: 'video', label: 'Video' },

  // Restauración
  { src: '/images/restauracion-1-laserena-santiago.webp', alt: 'Restauración — Trabajo 1', categoria: 'restauracion', tipo: 'img' },
  { src: '/images/restauracion-2-laserena-santiago.webp', alt: 'Restauración — Trabajo 2', categoria: 'restauracion', tipo: 'img' },
  { src: '/images/restauracion-3-laserena-santiago.webp', alt: 'Restauración — Trabajo 3', categoria: 'restauracion', tipo: 'img' },
  { src: '/images/restauracion-4-laserena-santiago.webp', alt: 'Restauración — Trabajo 4', categoria: 'restauracion', tipo: 'img' },
  { src: '/images/techo-feo-baño2.webp',    alt: 'Restauración — Techo con Humedad',  categoria: 'restauracion', tipo: 'img',   label: 'Antes' },
  { src: '/images/techo-lindo-baño-2.webp', alt: 'Restauración — Techo Restaurado',   categoria: 'restauracion', tipo: 'img',   label: 'Después' },
  { src: '/images/techo-feo-baño-1.mp4',    alt: 'Restauración — Video Antes',        categoria: 'restauracion', tipo: 'video', label: 'Antes' },
  { src: '/images/techo-lindo-baño-1.mp4',  alt: 'Restauración — Video Después',      categoria: 'restauracion', tipo: 'video', label: 'Después' },
  { src: '/images/video-techo.mp4',         alt: 'Restauración — Video Proceso',      categoria: 'restauracion', tipo: 'video', label: 'Video' },
];

const categorias = [
  { key: 'cabana',       label: 'Cabaña' },
  { key: 'casa',         label: 'Casa' },
  { key: 'oficina',      label: 'Oficina' },
  { key: 'edificio',     label: 'Edificio' },
  { key: 'restauracion', label: 'Restauración' },
];

const catLabels: Record<string, string> = {
  'cabana':       'Cabaña',
  'casa':         'Casa',
  'oficina':      'Oficina',
  'edificio':     'Edificio',
  'restauracion': 'Restauración',
};

// All categories grouped for stacked sliders
const grouped = categorias.map(c => ({
  key:   c.key,
  label: c.label,
  items: proyectos.filter(p => p.categoria === c.key),
}));
---

<section id="proyectos" class="section-pad" style="background-color: var(--color-bg-dark-2);">
  <div class="max-w-6xl mx-auto px-5">

    <!-- Header -->
    <div class="text-center mb-10 reveal">
      <p class="text-xs tracking-widest uppercase mb-3 font-bold" style="color: var(--color-accent); font-family: var(--font-label);">Galería de trabajos</p>
      <h2 class="text-3xl md:text-5xl font-black mb-3" style="font-family: var(--font-display); color: var(--color-text-light); letter-spacing: -0.02em;">
        Proyectos Realizados
      </h2>
      <p class="text-base max-w-xl mx-auto" style="color: rgba(240,232,212,0.6);">
        Trabajos en la Región Metropolitana y la Cuarta Región de Coquimbo. Cada obra, una transformación real.
      </p>
    </div>

    <!-- Navegación rápida por categoría -->
    <div class="flex flex-wrap justify-center gap-2 md:gap-3 mb-12 reveal delay-1">
      {categorias.map((cat) => (
        <a
          href={`#cat-${cat.key}`}
          class="nav-cat-btn px-4 md:px-6 py-2.5 md:py-3 rounded-full text-xs md:text-sm font-bold tracking-wide transition-all"
          style="border: 1.5px solid rgba(166,149,128,0.35); color: var(--color-accent-mid); font-family: var(--font-label); text-decoration: none;"
          data-cat={cat.key}
        >
          {cat.label}
        </a>
      ))}
    </div>

    <!-- ═══ TODAS LAS CATEGORÍAS VISIBLES ═══ -->
    <div class="flex flex-col gap-14">
      {grouped.map((group) => (
        <div id={`cat-${group.key}`} class="reveal">
          <!-- Etiqueta categoría -->
          <div class="flex items-center gap-4 mb-5">
            <h3 class="text-sm font-bold tracking-widest uppercase" style="color: var(--color-accent-mid); font-family: var(--font-label);">
              {group.label}
            </h3>
            <div class="flex-1 h-px" style="background: rgba(166,149,128,0.2);"></div>
            <span class="text-[10px]" style="color: rgba(166,149,128,0.45); font-family: var(--font-label);">{group.items.length} trabajos</span>
          </div>
          <!-- Slider horizontal -->
          <div class="cat-slider-wrapper px-1 relative group">
            <button class="slider-arrow left opacity-0 group-hover:opacity-100 transition-opacity hidden md:flex" data-dir="left" data-target={group.key}>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
            </button>
            <div class="cat-slider" id={`slider-${group.key}`}>
              {group.items.map((p) => (
                <div
                  class="cat-slider-item"
                  style="border: 1px solid rgba(166,149,128,0.15);"
                  data-src={p.src}
                  data-tipo={p.tipo}
                  data-alt={p.alt}
                >
                  {p.tipo === 'img' ? (
                    <img src={p.src} alt={p.alt} loading="lazy" decoding="async" />
                  ) : (
                    <video muted loop preload="metadata">
                      <source src={p.src} type="video/mp4" />
                    </video>
                  )}
                  <div
                    class="absolute inset-0 flex items-end p-3 opacity-0 hover:opacity-100 transition-opacity duration-300"
                    style="background: linear-gradient(to top, rgba(14,23,24,0.85) 0%, transparent 55%); pointer-events: none;"
                  >
                    <span class="text-[10px] font-bold" style="color: var(--color-accent-mid); font-family: var(--font-label);">
                      {p.label ?? catLabels[p.categoria]}
                    </span>
                  </div>
                  {p.tipo === 'video' && (
                    <div class="absolute top-2 right-2 px-2 py-1 rounded text-[10px] font-bold" style="background: rgba(14,23,24,0.85); color: var(--color-accent-mid); font-family: var(--font-label);">
                      ▶ Video
                    </div>
                  )}
                  {p.label && (
                    <div
                      class="absolute top-2 left-2 px-2 py-1 rounded text-[10px] font-bold"
                      style={`font-family: var(--font-label); background: ${p.label === 'Antes' ? 'rgba(220,60,60,0.8)' : p.label === 'Después' ? 'rgba(37,160,80,0.8)' : 'rgba(14,23,24,0.8)'}; color: #fff;`}
                    >
                      {p.label}
                    </div>
                  )}
                </div>
              ))}
            </div>
            <button class="slider-arrow right opacity-0 group-hover:opacity-100 transition-opacity hidden md:flex" data-dir="right" data-target={group.key}>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
            </button>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <div class="text-center mt-12 anim-up">
      <p class="text-base mb-5" style="color: rgba(240,232,212,0.65);">Transformamos espacios con eficiencia y calidad.</p>
      <a
        href={`https://wa.me/56995088570?text=${encodeURIComponent('Hola Salvatierra Paintworks, vi tus proyectos en la web y me gustaría cotizar mi trabajo')}`}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-3 px-11 py-6 rounded-full text-lg font-bold btn-wa-glow"
        style="background-color: var(--color-wa-green); color: var(--color-bg-dark); font-family: var(--font-label);"
      >
        <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
        </svg>
        Cotizar Proyecto
      </a>
    </div>
  </div>
</section>

<!-- Lightbox -->
<div id="lightbox" role="dialog" aria-modal="true" aria-label="Vista ampliada">
  <button id="lightbox-close" aria-label="Cerrar">&times;</button>
  <div id="lightbox-content" class="cursor-pointer"></div>
</div>

<script>
  // ── Highlight nav botón activo al hacer scroll ──
  const navBtns = document.querySelectorAll<HTMLAnchorElement>('.nav-cat-btn');
  const catSections = document.querySelectorAll<HTMLElement>('[id^="cat-"]');

  const sectionObserver = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        const key = e.target.id.replace('cat-', '');
        navBtns.forEach(b => {
          const isActive = b.dataset.cat === key;
          b.style.background = isActive ? 'var(--color-accent)' : '';
          b.style.color = isActive ? 'var(--color-bg-dark)' : '';
          b.style.borderColor = isActive ? 'var(--color-accent)' : '';
        });
      }
    });
  }, { threshold: 0.4 });

  catSections.forEach(s => sectionObserver.observe(s));

  // ── Lightbox ────────────────────────────────────
  const lightbox        = document.getElementById('lightbox')!;
  const lightboxContent = document.getElementById('lightbox-content')!;

  function openLightbox(src: string, alt: string, tipo: string) {
    lightboxContent.innerHTML = '';
    if (tipo === 'video') {
      const v = document.createElement('video');
      v.src = src; v.controls = true; v.autoplay = true;
      v.style.maxWidth = '95vw'; v.style.maxHeight = '85vh';
      lightboxContent.appendChild(v);
    } else {
      const img = document.createElement('img');
      img.src = src; img.alt = alt;
      lightboxContent.appendChild(img);
    }
    lightbox.classList.add('open');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.classList.remove('open');
    lightboxContent.innerHTML = '';
    document.body.style.overflow = '';
  }

  // Cerrar al clickear en el contenido (imagen o video)
  lightboxContent.addEventListener('click', closeLightbox);

  // Click on slider items
  document.querySelectorAll<HTMLElement>('.cat-slider-item').forEach(el => {
    el.addEventListener('click', () => openLightbox(el.dataset.src!, el.dataset.alt!, el.dataset.tipo!));
  });

  document.getElementById('lightbox-close')!.addEventListener('click', closeLightbox);
  lightbox.addEventListener('click', e => { if (e.target === lightbox) closeLightbox(); });
  document.addEventListener('keydown', e => { if (e.key === 'Escape') closeLightbox(); });

  // ── Slider Navigation Buttons ───────────────────
  const arrows = document.querySelectorAll<HTMLButtonElement>('.slider-arrow');
  arrows.forEach(arrow => {
    arrow.addEventListener('click', () => {
      const dir = arrow.dataset.dir;
      const targetId = `slider-${arrow.dataset.target}`;
      const slider = document.getElementById(targetId);
      if (slider) {
        const scrollAmount = slider.clientWidth * 0.8;
        slider.scrollBy({
          left: dir === 'left' ? -scrollAmount : scrollAmount,
          behavior: 'smooth'
        });
      }
    });
  });
</script>
